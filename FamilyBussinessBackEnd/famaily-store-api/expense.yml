info:
  name: expense
  type: http

http:
  method: POST
  url: http://localhost:8080/api/cash-flow/expense
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​